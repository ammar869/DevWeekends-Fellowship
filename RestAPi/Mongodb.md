Now we are going to connect with the mongo db 
npm i mongoose

for connection:

we made the schema (structure)
-- then by using that schema we made the --Model
----then by using model we will we do crud operations


const newSchema = new mongooe.Schema({},{},{})