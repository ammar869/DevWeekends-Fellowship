instead of we are doing http//http://localhost:8000/users
we will do the http://localhost:8000/api/users
why ?
bcz we are making the server for the mobiles, web browsers , others also
so we are making an hybrid server 
if desktop wants the api(data) then we will return him the renered html page but
if mobile want that so we will give him the JSON
so /api/users means we are giving the JSON DATA
and so /users means we are giving the HTML PAGE